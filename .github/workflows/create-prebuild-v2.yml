
name: Create codespaces prebuild v2
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  createPrebuildV2:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - uses: github/codespaces-precache@v1.0.1
        with:
          regions: WestUs2
          sku_name: standardLinux32gb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
